import { FormatTexPipe } from './format-tex.pipe';

describe('FormatTexPipe', () => {
  it('create an instance', () => {
    const pipe = new FormatTexPipe();
    expect(pipe).toBeTruthy();
  });
});
